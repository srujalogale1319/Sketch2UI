# Sketch2UI
A browser-based drag-and-drop UI wireframing tool built with React + Vite.
It allows users to design interfaces visually, move/resize elements, customize styles, and automatically generate HTML, CSS, and Tailwind CSS code for each component.

🚀 Features
Drag & Drop Canvas – Add, move, and resize UI blocks easily.
Resizable Components – Corner and side handles like Figma.
Custom Styling Panel
Width & height
Background color
Border color & width
Element type
Auto Code Generation
HTML
CSS
Tailwind CSS
One-click code copy
Persistent UI State (optional if using localStorage).
Clean React + Tailwind Architecture.

📦 Tech Stack
React (Vite)
TailwindCSS
Lucide Icons
JavaScript / JSX

🛠️ Installation & Setup
1. Clone the repository
git clone https://github.com/<your-username>/wireframe-builder.git
cd wireframe-builder

2. Install dependencies
npm install

3. Start development server
npm run dev

App will run at:
http://localhost:5173
